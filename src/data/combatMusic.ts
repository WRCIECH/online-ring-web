// YouTube video IDs used as the default background-music playlist.
// Empty by default — no bundled tracks ship with the game. Add your own via
// the in-game Music settings overlay, or replace this list directly.
export const DEFAULT_MUSIC_TRACKS: string[] = []
